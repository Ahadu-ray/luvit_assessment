import 'package:flutter/material.dart';

class CurrentCardIndicator extends StatelessWidget {
  const CurrentCardIndicator({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}