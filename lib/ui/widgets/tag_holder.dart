import 'package:flutter/material.dart';

class TagHolder extends StatelessWidget {
  const TagHolder({required this.name, super.key});

  final String name;

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}